---
title: "Installation guide"
description: "Installation instructions for setting up Hugo and GitHub Pages"
weight: 1
draft: false
---

### Installation of node.js and Hugo

#### Windows

1. Install Node.js from [nodejs.org](https://nodejs.org/en/download).
2. Then install Hugo using WinGet package manager. Open Powershell as an Administrator and run:

```bash
winget install Hugo.Hugo.Extended
```

#### macOS

1. Install Homebrew if you haven't already. Open Terminal and run:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

1. Install Node.js using Homebrew:

```bash
brew install node
```

1. Install Hugo using Homebrew:

```bash
brew install hugo
```
